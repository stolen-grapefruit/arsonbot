#IK.py

from config import L1, L2, L3, L4
import numpy as np

def compute_IK(x, y, z):
    """
    Compute inverse kinematics for the 4R arm.
    Args:
        x, y, z: desired end-effector position in workspace

    Returns:
        q: np.array of joint angles [q1, q2, q3, q4] in radians
    """
    # Placeholder: replace with your logic
    q = np.zeros(4)
    return q
